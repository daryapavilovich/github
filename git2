#include <stdio.h>
int main() {
	int A[100];
	for (int i = 1; i < 100; i = i+2)
	{
		A[101 - i] = i;
		printf("%d\n", A[101-i]=i);
	}
	for (int i = 100; i > 0; i = i-2)
	{
		A[101 - i] = i;
	ptintf("%d\n", A[101-i]=i);
}
return 0;
}
